using UnityEngine;
using System.Collections;
using System.Collections.Generic;
using LOS;

public class TakeDamage : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
//		print(GetComponentInChildren<L>().IsVisible());

//		print(GetComponentInChildren<LOSVisibilityInfo>().VisibleSources.Count);
//
//		foreach(LOSSource source in GetComponentInChildren<LOSVisibilityInfo>().VisibleSources){
//			print(source.gameObject.transform.parent.name);
//		}
	}
}
